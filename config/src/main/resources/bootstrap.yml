server:
  port: 8003

spring:
  application:
    name: vaps-config
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8002/eureka/